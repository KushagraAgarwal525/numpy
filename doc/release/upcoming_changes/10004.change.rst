``datetime64`` accepts NumPy integer scalars
--------------------------------------------
``np.datetime64`` can now be constructed from NumPy integer scalars and
0-D integer arrays when an explicit datetime unit is provided, matching
construction from Python integers.
